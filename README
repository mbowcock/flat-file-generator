C# Flat file generator (fixed width file generator)

1. Create a class to describe a file record
2. Apply FlatFileAttribute to class properties
3. Call FlatFile.writeFile(...)

FlatFile.writeFile(List<T> records, string path) takes a list of objects (your record class) and creates a file using the
FlatFileAttributes in the format you specified. File is written to the path you pass.
